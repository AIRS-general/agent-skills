# Oh My ZSH

## `p10k` Problem with icons.

Icons do not show up in vscode terminal.

If the Apple logo () shows in macOS Terminal/iTerm but not in the VS Code integrated terminal, it’s almost always a font mismatch.

`cmd + shit + P` -> Open User Settings (Json), then set `"terminal.integrated.fontFamily": "MesloLGS NF"`
